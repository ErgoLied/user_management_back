module.exports = {
    CONFIG: require('./config'),
    REG_EX: require('./regex'),

    ERR_MSG: require('./error-messages.enum'),
    STATUS_CODE: require('./status-codes'),

    USER_ROLES: require('./user-roles.enum')
};
